---
import { TAGS } from "@/utils/tags";
import LinkButton from "./LinkButton.astro";
import Link from "@/assets/icons/Link.astro";

const FREELANCES = [
  {
    title: "Luang Asociados SAC",
    description:
      "Sitio web para la empresa Luang Asociados SAC, incluye varias secciones y un formulario de contacto.",
    link: "https://luang.com.pe/",
    image: "/images/luang.webp",
    tags: [TAGS.ASTRO, TAGS.TAILWIND, TAGS.TYPESCRIPT],
  },
  {
    title: "Atemporal",
    description:
      "Biolink para la empresa Atemporal, diseño minimalista y moderno.",
    link: "https://atemporalarq.vercel.app/",
    image: "/images/atemporal.webp",
    tags: [TAGS.REACT, TAGS.TAILWIND, TAGS.TYPESCRIPT],
  },
  {
    title: "Blu Café",
    description:
      "Biolink para la empresa Blu Café, diseño minimalista y moderno.",
    link: "https://blucafe.vercel.app/",
    image: "/images/blucafe.webp",
    tags: [TAGS.ASTRO, TAGS.TAILWIND, TAGS.TYPESCRIPT],
  },
];
---

<div class="flex flex-col gap-y-16">
  {
    FREELANCES.map(({ image, title, description, link, tags }, index) => (
      <article class="group flex flex-col gap-8 md:flex-row md:gap-10 md:items-center">
        <!-- Image -->
        <div class="w-full md:w-1/2 flex-shrink-0">
          <div class="relative overflow-hidden rounded-2xl bg-gray-100 dark:bg-gray-900 border border-gray-200/80 dark:border-gray-800 transition-all duration-500 group-hover:border-gray-300 dark:group-hover:border-gray-700 group-hover:shadow-xl dark:group-hover:shadow-black/50">
            <img
              alt={`Portada del proyecto ${title}`}
              class="w-full h-52 sm:h-64 object-cover object-top transition duration-500 group-hover:scale-[1.03]"
              loading="lazy"
              decoding="async"
              src={image}
            />
          </div>
        </div>

        <!-- Content -->
        <div class="w-full md:w-1/2 flex flex-col justify-center">
          <!-- Number -->
          <span class="font-mono text-xs font-bold text-gray-300 dark:text-gray-700 tracking-widest mb-2 select-none">
            {String(index + 1).padStart(2, "0")}
          </span>

          <!-- Title -->
          <h3 class="text-2xl font-bold text-gray-900 dark:text-white mb-3">
            {title}
          </h3>

          <!-- Tags -->
          <ul class="flex flex-wrap gap-2 mb-4">
            {tags.map((tag) => (
              <li>
                <span
                  class={`flex items-center gap-1.5 rounded-full text-[11px] font-medium ${tag.class} px-2.5 py-1`}
                >
                  <tag.icon class="size-3" />
                  {tag.name}
                </span>
              </li>
            ))}
          </ul>

          <!-- Description -->
          <p class="text-[0.925rem] text-gray-600 dark:text-gray-400 leading-relaxed mb-5">
            {description}
          </p>

          <!-- Button -->
          <div class="flex items-center gap-3">
            {link && (
              <LinkButton href={link}>
                <Link class="size-4" />
                Ver proyecto
              </LinkButton>
            )}
          </div>
        </div>
      </article>
    ))
  }
</div>
